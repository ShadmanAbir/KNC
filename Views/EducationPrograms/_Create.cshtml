@model KNC.ViewModels.EducationProgramsViewModel

<div class="modal fade" id="modalForm" tabindex="-1" aria-labelledby="modalFormLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            @using (Html.BeginForm("Create", "EducationPrograms", FormMethod.Post, new { @class = "form" }))
            {
                <div class="modal-header">
                    <h4 class="modal-title" id="modalFormLabel">Create Education Program</h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    @Html.HiddenFor(m => m.EducationProgramID)
                    <div class="row g-3">
                        <div class="col-md-12">
                            @Html.LabelFor(m => m.ProgramName, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.ProgramName, new { @class = "form-control" })
                        </div>
                        <div class="col-md-12">
                            @Html.LabelFor(m => m.Duration, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.Duration, new { @class = "form-control" })
                        </div>
                        <div class="col-md-12">
                            @Html.LabelFor(m => m.ShortName, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.ShortName, new { @class = "form-control" })
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-success">Save</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                </div>
            }
        </div>
    </div>
</div>